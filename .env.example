# Database Configuration
# Leave DATABASE_URL empty for local development (uses local SQLite fallback)
# For production (Vercel), add your PostgreSQL connection string here.
DATABASE_URL="postgres://user:password@host:port/database"

# Application Settings
PORT=3001
NODE_ENV="development"

# Security
# Required for accessing the admin dashboard API
ADMIN_PASSWORD="your_secure_password"
